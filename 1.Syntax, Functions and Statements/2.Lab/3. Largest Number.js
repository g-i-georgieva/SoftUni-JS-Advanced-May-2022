function solve(p1, p2, p3) {
    // let largest = Math.max(p1, p2, p3)
    // console.log(`The largest number is ${largest}.`);

    return `The largest number is ${Math.max(p1, p2, p3)}.`
}
console.log(solve(5, -3, 16))